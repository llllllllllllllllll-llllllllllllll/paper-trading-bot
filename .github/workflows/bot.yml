name: Paper Trading Bot

on:
  workflow_dispatch:
  schedule:
    - cron: "28 * * * *"   # 2 min before candle close  (:30)
    - cron: "30 * * * *"   # exactly at candle close
    - cron: "32 * * * *"   # 2 min after candle open

permissions:
  contents: write

concurrency:
  group: paper-trading-bot
  cancel-in-progress: false   # never cancel a running bot mid-execution

jobs:
  run-bot:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      PYTHONUNBUFFERED: "1"
      STATE_FILE: state.json

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run paper trading bot
        run: python paper_bot.py

      - name: Save state to repo
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add state.json
          git commit -m "chore: update state [skip ci]" || echo "No changes to commit"
          git pull --rebase origin "${GITHUB_REF_NAME}"
          git push
